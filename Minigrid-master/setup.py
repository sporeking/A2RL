"""Setups the project."""

from setuptools import setup
setup(name='minigrid',
      version='0.1',
      description='minigrid environment',
      license='Apache License',
      packages=['minigird'],
      zip_safe=False)